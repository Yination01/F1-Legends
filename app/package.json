{
  "name": "f1-clash-zero-app",
  "private": true,
  "scripts": {
    "copy-game": "node copy-game.js",
    "sync": "npm run copy-game && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/preferences": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.9"
  }
}
